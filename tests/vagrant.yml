# test file for oracle-java
---
- hosts: all
  remote_user: vagrant
  become: true
  roles:
    - ../../
  vars:
    oracle_java_versions:
      - version: 8
      - version: 9
        set_as_default: true
        update_version: '9b171-1~webupd8~0'
