# test file for oracle-java
---
- hosts: localhost
  connection: local
  become: true
  roles:
    - ../../
  vars:
    oracle_java_versions:
      - version: 8
      - version: 9
        set_as_default: true
